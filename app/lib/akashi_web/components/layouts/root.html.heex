<!DOCTYPE html>
<%# <html lang="en" class="[scrollbar-gutter:stable]"> %>
<html lang="en" class="">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title>
      <%= assigns[:page_title] || "Akashi" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <header class="flex flex-row p-6 justify-between align-middle w-full">
      <div class="flex flex-row align-middle text-center">
        <.link
          href={~p"/"}
          class="text-3xl italic font-semibold font-mono"
        >
          Akashi
        </.link>
      </div>
      <%# <div class="flex flex-row space-x-4"> %>
      <ul class="flex flex-row space-x-4">
        <%= if @current_user do %>
          <li class="text-2xl">
            <%= @current_user.email %>
          </li>
          <li>
            <.link
              href={~p"/users/settings"}
              class="btn"
            >
              Settings
            </.link>
          </li>
          <li>
            <.link
              href={~p"/users/log_out"}
              method="delete"
              class="btn"
            >
              Log out
            </.link>
          </li>
        <% else %>
          <.link
            href={~p"/users/log_in"}
            class="btn"
          >
            Log in
          </.link>
        <% end %>
        <%= live_render(@conn, Akashi.WalletLive, id: "wallet") %>
      </ul>
    <%# </div> %>
  </header>
  <body class="h-screen">
    <%= @inner_content %>
  </body>
</html>
